apiVersion: v1
kind: Service
metadata:
  name: {{ include "space-hackability.fullname" . }}
  labels:
    {{- include "space-hackability.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "space-hackability.selectorLabels" . | nindent 4 }}
